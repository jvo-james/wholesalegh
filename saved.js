(async()=>{await WGH.loadProducts();const grid=document.querySelector('[data-saved-grid]'),empty=document.querySelector('[data-saved-empty]'),count=document.querySelector('[data-saved-count]');const read=()=>{try{return JSON.parse(localStorage.getItem('wgh_wishlist')||'[]')}catch{return[]}};function render(){const ids=read(),list=ids.map(id=>WGH.products.find(p=>p.id===id)).filter(Boolean);count.textContent=`${list.length} saved piece${list.length===1?'':'s'}`;grid.innerHTML=list.map(p=>WGH.productCard(p)).join('');empty.hidden=!!list.length;grid.hidden=!list.length;WGH.bindProductCards(grid);grid.querySelectorAll('[data-wishlist]').forEach(b=>b.addEventListener('click',()=>setTimeout(render,10),{once:true}))}document.querySelector('[data-clear-saved]').onclick=()=>{localStorage.removeItem('wgh_wishlist');render();WGH.showToast('Saved pieces cleared.','success')};render()})();