(async()=>{const grid=document.querySelector('[data-shop-grid]');if(!grid)return;await WGH.loadProducts();const params=new URLSearchParams(location.search);let mode=params.get('mode')==='wholesale'?'wholesale':'retail',category=params.get('category')||'all',sort=params.get('sort')||'featured',query='',cycleTimer=null,activeCard=null,scrolling=false,scrollTimer=null;const titles={all:'Shop all',new:'New arrivals',tops:'Tops',dresses:'Dresses',pants:'Pants',basics:'Basics'};
function setupCycling(){if(cycleTimer)clearInterval(cycleTimer);if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const obs=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting&&e.intersectionRatio>.72).sort((a,b)=>b.intersectionRatio-a.intersectionRatio);activeCard=visible[0]?.target||activeCard},{threshold:[.72,.85,.95]});grid.querySelectorAll('[data-product-card]').forEach(c=>obs.observe(c));cycleTimer=setInterval(()=>{if(scrolling||!activeCard||activeCard._wghTouched?.())return;const btns=[...activeCard.querySelectorAll('[data-card-colour]')];if(btns.length<2)return;let i=btns.findIndex(b=>b.classList.contains('active'));activeCard._wghSetColour?.(btns[(i+1)%btns.length])},3800)}
function render(){let list=[...WGH.products].filter(p=>(category==='all'||(category==='new'?p.isNew:p.category===category))&&(!query||`${p.name} ${p.category} ${(p.colours||[]).join(' ')}`.toLowerCase().includes(query)));if(sort==='price-low')list.sort((a,b)=>(mode==='wholesale'?a.wholesalePrice:a.retailPrice)-(mode==='wholesale'?b.wholesalePrice:b.retailPrice));if(sort==='price-high')list.sort((a,b)=>(mode==='wholesale'?b.wholesalePrice:b.retailPrice)-(mode==='wholesale'?a.wholesalePrice:a.retailPrice));if(sort==='newest')list.sort((a,b)=>Number(b.isNew)-Number(a.isNew));grid.innerHTML=list.map(p=>WGH.productCard(p,mode)).join('');WGH.bindProductCards(grid);document.querySelector('[data-product-count]').textContent=list.length;document.querySelector('[data-empty-state]').hidden=!!list.length;document.querySelector('[data-shop-title]').textContent=mode==='wholesale'&&category==='all'?'Wholesale':titles[category]||'Shop all';document.querySelector('[data-shop-subtitle]').textContent=mode==='wholesale'?'Build each style from 6 pieces and mix available colours and sizes within your minimum.':'Made-to-order pieces produced after purchase and tracked from production to delivery.';const toolbar=document.querySelector('.shop-toolbar');if(toolbar){const box=document.createElement('label');box.className='shop-search';box.innerHTML='<i class="fa-solid fa-magnifying-glass"></i><input type="search" placeholder="Search styles or colours" aria-label="Search products"><div class="shop-search-suggestions" hidden></div>';toolbar.appendChild(box);const input=box.querySelector('input'),suggest=box.querySelector('div');input.addEventListener('input',()=>{query=input.value.trim().toLowerCase();const matches=WGH.products.filter(p=>`${p.name} ${(p.colours||[]).join(' ')}`.toLowerCase().includes(query)).slice(0,4);suggest.innerHTML=query?matches.map(p=>`<a href="product.html?id=${p.id}&mode=${mode}">${p.name}<small>${(p.colours||[]).slice(0,3).join(' · ')}</small></a>`).join(''):'';suggest.hidden=!query||!matches.length;render()});input.addEventListener('blur',()=>setTimeout(()=>suggest.hidden=true,160));} 
document.querySelectorAll('[data-mode]').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));document.querySelectorAll('[data-category]').forEach(b=>b.classList.toggle('active',b.dataset.category===category));const url=new URL(location.href);mode==='retail'?url.searchParams.delete('mode'):url.searchParams.set('mode',mode);category==='all'?url.searchParams.delete('category'):url.searchParams.set('category',category);sort==='featured'?url.searchParams.delete('sort'):url.searchParams.set('sort',sort);history.replaceState({},'',url);setupCycling()}
document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;render()});document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>{category=b.dataset.category;render()});const pop=document.querySelector('[data-sort-popover]');document.querySelector('[data-sort-toggle]').onclick=()=>pop.hidden=!pop.hidden;pop.querySelectorAll('[data-sort]').forEach(b=>b.onclick=()=>{sort=b.dataset.sort;pop.hidden=true;render()});window.addEventListener('scroll',()=>{scrolling=true;clearTimeout(scrollTimer);scrollTimer=setTimeout(()=>scrolling=false,220)},{passive:true});render();try{const ids=JSON.parse(localStorage.getItem('wgh_recently_viewed')||'[]'),recent=WGH.products.filter(p=>ids.includes(p.id)).sort((a,b)=>ids.indexOf(a.id)-ids.indexOf(b.id)).slice(0,5),sec=document.querySelector('[data-shop-recent]'),root=document.querySelector('[data-shop-recent-list]');if(recent.length&&sec&&root){sec.hidden=false;root.innerHTML=recent.map(p=>WGH.productCard(p,mode)).join('');WGH.bindProductCards(root)}}catch{}})();
