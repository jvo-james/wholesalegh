(()=>{const form=document.querySelector('[data-track-page-form]'),search=document.querySelector('[data-track-search]'),state=document.querySelector('[data-track-result-state]'),out=document.querySelector('[data-track-page-result]'),params=new URLSearchParams(location.search);if(params.get('order'))form.elements.orderNumber.value=params.get('order');if(params.get('email'))form.elements.email.value=params.get('email');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function items(order){return (order.items||[]).map(i=>`<article class="tracked-item"><img src="${esc(i.image||i.images?.[0]||'')}" alt=""><div><strong>${esc(i.name||'Item')}</strong><span>${Number(i.totalQuantity||1)} piece${Number(i.totalQuantity||1)===1?'':'s'}</span><small>${(i.variants||[]).map(v=>`${v.quantity}× ${esc(v.colour||v.color||'')} / ${esc(v.size||'')}`).join(' · ')}</small></div></article>`).join('')}function journey(order){const current=Math.max(0,WGH.statuses.indexOf(order.status));return `<div class="journey-card"><div class="journey-current"><span>Current stage</span><strong>${WGH.statusLabels[order.status]||'In progress'}</strong><p>${order.estimatedDelivery?`Estimated delivery ${esc(order.estimatedDelivery)}`:'Your delivery estimate will update as production progresses.'}</p></div><div class="journey-rail">${WGH.statuses.map((s,i)=>`<div class="journey-stop ${i<current?'done':i===current?'current':''}"><span class="journey-dot">${i<current?'<i class="fa-solid fa-check"></i>':''}</span><div><strong>${WGH.statusLabels[s]}</strong>${i===current?'<small>We are here</small>':''}</div></div>`).join('')}</div></div>`}async function run(){const btn=form.querySelector('button'),data=Object.fromEntries(new FormData(form));await WGH.withLoading(btn,async()=>{try{const order=await WGH.api('/track',data);const u=new URL(location.href);u.searchParams.set('order',data.orderNumber.trim().toUpperCase());u.searchParams.set('email',data.email.trim());history.replaceState({},'',u);sessionStorage.setItem('wgh_tracking_state',JSON.stringify({order:data.orderNumber,email:data.email}));search.hidden=true;state.hidden=false;out.innerHTML=`<div class="tracking-result-head"><div><button class="tracking-back" type="button" data-track-back><i class="fa-solid fa-arrow-left"></i> Track another order</button><p class="eyebrow">${esc(order.orderNumber)}</p><h2>Your order is moving.</h2></div><div class="tracking-batch"><span>Production cycle</span><strong>${esc(order.batchName||'Assigned')}</strong></div></div><section class="tracked-items"><div class="tracking-section-title"><span>Your pieces</span><strong>${order.pieces||0} total</strong></div>${items(order)}</section>${journey(order)}<div class="tracking-ctas"><a class="button button-dark" href="shop.html"><i class="fa-solid fa-bag-shopping"></i> Continue shopping</a><a class="button button-outline" href="account.html">View my account</a></div>`;out.querySelector('[data-track-back]').addEventListener('click',()=>{state.hidden=true;search.hidden=false;const u=new URL(location.href);u.search='';history.replaceState({},'',u);window.scrollTo({top:0,behavior:'smooth'})})}catch(err){search.hidden=false;state.hidden=true;WGH.showToast('We could not find that order. Check the order number and email address.')}},'Finding order')}form.addEventListener('submit',e=>{e.preventDefault();run()});if(params.get('order')&&params.get('email'))setTimeout(run,250)})();
