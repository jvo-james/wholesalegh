(async()=>{await WGH.loadProducts();const params=new URLSearchParams(location.search),product=WGH.products.find(p=>p.id===params.get('id'))||WGH.products[0];if(!product)return;let mode=params.get('mode')==='wholesale'?'wholesale':'retail';const colourSlug=v=>String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');let requestedColour=params.get('colour'),colour=product.colours.find(c=>c===requestedColour||colourSlug(c)===colourSlug(requestedColour))||product.colours[0],size=product.sizes[1]||product.sizes[0],qty=1,variants=[{colour:product.colours[0],size:product.sizes[1]||product.sizes[0],quantity:1}];const $=s=>document.querySelector(s),colourHex=c=>product.colourHexes?.[c]||WGH.colourValue(c);document.title=`${product.name} | The Wholesale Ghana`;try{let rv=JSON.parse(localStorage.getItem('wgh_recently_viewed')||'[]').filter(x=>x!==product.id);rv.unshift(product.id);localStorage.setItem('wgh_recently_viewed',JSON.stringify(rv.slice(0,8)))}catch{}$('[data-product-name]').textContent=product.name;$('[data-breadcrumb-name]').textContent=product.name;$('[data-product-description]').textContent=product.description||'';$('[data-product-category]').textContent=product.category||'Collection';$('[data-product-details]').textContent=product.details||'';
function gallery(){const imgs=(product.colourImages?.[colour]?.length?product.colourImages[colour]:product.images)||[];const root=$('[data-product-gallery]');const special=product.id==='sculpted-high-neck-hugger-dress'&&colour==='Black'&&imgs.length>1;if(special){root.innerHTML=`<div class="special-black-gallery" data-special-gallery><figure class="single-product-image"><img src="${imgs[0]||''}" alt="${product.name} in ${colour}"></figure><figure class="single-product-image special-model-image"><img src="${imgs[1]||''}" alt="${product.name} Black styled on model"></figure></div><div class="special-gallery-pagination" aria-label="Product image position"><button type="button" class="active" aria-label="View product image"></button><button type="button" aria-label="View styled image"></button></div>`;requestAnimationFrame(()=>{const track=root.querySelector('[data-special-gallery]'),dots=[...root.querySelectorAll('.special-gallery-pagination button')];if(!track||!dots.length)return;let raf=0;track.addEventListener('scroll',()=>{cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{const i=Math.max(0,Math.min(dots.length-1,Math.round(track.scrollLeft/Math.max(1,track.clientWidth))));dots.forEach((d,n)=>d.classList.toggle('active',n===i))})},{passive:true});dots.forEach((d,i)=>d.onclick=()=>track.scrollTo({left:track.clientWidth*i,behavior:'smooth'}))})}else{const src=imgs[0]||'';root.innerHTML=`<figure class="single-product-image"><img src="${src}" alt="${product.name} in ${colour}"></figure>`}}$('[data-colour-options]').innerHTML=product.colours.map(c=>`<button type="button" class="colour-option" data-colour="${c}"><i style="background:${colourHex(c)}"></i>${c}</button>`).join('');$('[data-size-options]').innerHTML=product.sizes.map(s=>`<button type="button" class="size-option" data-size="${s}">${s}</button>`).join('');
function render(){document.querySelectorAll('[data-purchase-mode]').forEach(b=>b.classList.toggle('active',b.dataset.purchaseMode===mode));$('[data-retail-builder]').hidden=mode!=='retail';$('[data-wholesale-builder]').hidden=mode!=='wholesale';$('[data-product-price]').textContent=mode==='wholesale'?(Number(product.wholesalePrice)>0?WGH.money(product.wholesalePrice):'Price pending'):WGH.money(product.retailPrice);$('[data-price-note]').textContent=mode==='wholesale'?(Number(product.wholesalePrice)>0?'Wholesale price per piece':'Wholesale price will be added by admin'):'Retail price';document.querySelectorAll('[data-colour]').forEach(b=>b.classList.toggle('active',b.dataset.colour===colour));document.querySelectorAll('[data-size]').forEach(b=>b.classList.toggle('active',b.dataset.size===size));$('[data-selected-colour]').textContent=colour;$('[data-qty]').textContent=qty;const u=new URL(location.href);u.searchParams.set('id',product.id);u.searchParams.set('mode',mode);u.searchParams.set('colour',colour.toLowerCase().replace(/\s+/g,'-'));history.replaceState({},'',u);gallery();renderVariants()}
function renderVariants(){const h=$('[data-variant-builder]');h.innerHTML=variants.map((v,i)=>`<div class="variant-row" data-variant-index="${i}"><select data-v-colour>${product.colours.map(c=>`<option ${c===v.colour?'selected':''}>${c}</option>`).join('')}</select><select data-v-size>${product.sizes.map(s=>`<option ${s===v.size?'selected':''}>${s}</option>`).join('')}</select><div class="mini-qty"><button type="button" data-v-minus>−</button><span>${v.quantity}</span><button type="button" data-v-plus>+</button></div><button type="button" class="remove-variant" data-v-remove>×</button></div>`).join('');const total=variants.reduce((s,v)=>s+v.quantity,0);$('[data-wholesale-count]').textContent=total;$('[data-moq-bar]').style.width=`${Math.min(100,total/product.moq*100)}%`;$('[data-moq-message]').textContent=total>=product.moq?`Minimum reached. Your ${total}-piece mix is ready.`:`Add ${product.moq-total} more piece${product.moq-total===1?'':'s'} to continue.`;h.querySelectorAll('.variant-row').forEach(r=>{const i=+r.dataset.variantIndex;r.querySelector('[data-v-colour]').onchange=e=>variants[i].colour=e.target.value;r.querySelector('[data-v-size]').onchange=e=>variants[i].size=e.target.value;r.querySelector('[data-v-minus]').onclick=()=>{variants[i].quantity=Math.max(1,variants[i].quantity-1);renderVariants()};r.querySelector('[data-v-plus]').onclick=()=>{variants[i].quantity++;renderVariants()};r.querySelector('[data-v-remove]').onclick=()=>{if(variants.length>1){variants.splice(i,1);renderVariants()}}})}
function miniCart(item){let d=document.querySelector('[data-mini-cart]');if(!d){d=document.createElement('aside');d.className='mini-cart-drawer';d.dataset.miniCart='';document.body.appendChild(d)}d.innerHTML=`<button class="mini-cart-close" type="button" aria-label="Close">×</button><p class="eyebrow">Added to bag</p><div class="mini-cart-item"><img src="${item.image}" alt=""><div><strong>${item.name}</strong><span>${item.totalQuantity} piece${item.totalQuantity===1?'':'s'} · ${item.orderType}</span><small>${item.variants.map(v=>`${v.quantity}× ${v.colour} / ${v.size}`).join(' · ')}</small></div></div><div class="mini-cart-total"><span>Bag total</span><strong>${WGH.money(WGH.getCart().reduce((s,i)=>s+i.unitPrice*i.totalQuantity,0))}</strong></div><a class="button button-dark full" href="cart.html">View full cart</a><button class="text-button" type="button" data-keep-shopping>Keep shopping</button>`;requestAnimationFrame(()=>d.classList.add('open'));d.querySelector('.mini-cart-close').onclick=d.querySelector('[data-keep-shopping]').onclick=()=>d.classList.remove('open')}
document.querySelectorAll('[data-purchase-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.purchaseMode;render()});document.querySelectorAll('[data-colour]').forEach(b=>b.onclick=()=>{colour=b.dataset.colour;render();if(matchMedia('(max-width:820px)').matches)setTimeout(()=>document.querySelector('[data-product-gallery]')?.scrollIntoView({behavior:'smooth',block:'start'}),80)});document.querySelectorAll('[data-size]').forEach(b=>b.onclick=()=>{size=b.dataset.size;render()});$('[data-qty-minus]').onclick=()=>{qty=Math.max(1,qty-1);render()};$('[data-qty-plus]').onclick=()=>{qty++;render()};$('[data-add-variant]').onclick=()=>{variants.push({colour:product.colours[0],size:product.sizes[0],quantity:1});renderVariants()};$('[data-add-to-bag]').onclick=()=>{if(mode==='wholesale'&&!(Number(product.wholesalePrice)>0))return WGH.showToast('Wholesale price for this style has not been published yet.');const total=mode==='wholesale'?variants.reduce((s,v)=>s+v.quantity,0):qty;if(mode==='wholesale'&&total<product.moq)return WGH.showToast(`Wholesale minimum is ${product.moq} pieces per style.`);const imgs=(product.colourImages?.[colour]?.length?product.colourImages[colour]:product.images)||[];const item={key:`${product.id}-${Date.now()}`,productId:product.id,name:product.name,image:imgs[0],orderType:mode,unitPrice:mode==='wholesale'?product.wholesalePrice:product.retailPrice,totalQuantity:total,variants:mode==='wholesale'?variants.map(v=>({...v})):[{colour,size,quantity:qty}]};const cart=WGH.getCart();cart.push(item);WGH.saveCart(cart);miniCart(item)};
if(product.available===false){const add=$('[data-add-to-bag]');add.disabled=true;add.textContent='Temporarily paused';const notify=document.createElement('form');notify.className='notify-me-form';notify.innerHTML='<p class="mini-label">Want this piece?</p><strong>Get an email when ordering reopens.</strong><div><input type="email" name="email" placeholder="Email address" required><button class="button button-outline" type="submit">Notify me</button></div>';add.insertAdjacentElement('afterend',notify);setTimeout(()=>{if(WGH.auth?.currentUser?.email&&!notify.elements.email.value)notify.elements.email.value=WGH.auth.currentUser.email},800);notify.onsubmit=async e=>{e.preventDefault();const btn=notify.querySelector('button'),email=notify.elements.email.value;await WGH.withLoading(btn,async()=>{await WGH.api('/notify',{email,productId:product.id});WGH.showToast('We’ll let you know when this piece is available.','success');notify.reset()},'Saving')}}
const details=$('[data-product-purchase] .product-sticky');const care=document.createElement('details');care.innerHTML=`<summary>Care instructions</summary><p>${product.care||'Follow the garment-care label supplied with your finished piece. Wash gently, avoid harsh bleach and store clean and dry.'}</p>`;details.appendChild(care);const save=document.createElement('button');save.type='button';save.className='product-save text-link';save.dataset.wishlist=product.id;save.innerHTML='<i class="fa-regular fa-heart"></i> Save for later';details.appendChild(save);WGH.bindProductCards(document);const related=WGH.products.filter(p=>p.id!==product.id).slice(0,4);$('[data-related-products]').innerHTML=related.map(p=>WGH.productCard(p,mode)).join('');WGH.bindProductCards($('[data-related-products]'));const share=document.createElement('button');share.className='product-share text-link';share.type='button';share.innerHTML='<i class="fa-solid fa-arrow-up-from-bracket"></i> Share this colour';share.onclick=async()=>{try{await navigator.share({title:product.name,url:location.href})}catch{await navigator.clipboard?.writeText(location.href);WGH.showToast('Product link copied.','success')}};$('[data-product-purchase] .product-sticky').appendChild(share);
try{const ids=JSON.parse(localStorage.getItem('wgh_recently_viewed')||'[]').filter(id=>id!==product.id),recent=WGH.products.filter(p=>ids.includes(p.id)).sort((a,b)=>ids.indexOf(a.id)-ids.indexOf(b.id)).slice(0,5),root=$('[data-recently-viewed]'),sec=$('[data-recently-viewed-section]');if(recent.length&&root&&sec){sec.hidden=false;root.innerHTML=recent.map(p=>WGH.productCard(p,mode)).join('');WGH.bindProductCards(root)}}catch{}
render();})();
